[project]
name = "coding-agents-sandbox"
version = "0.1.0"
description = "E2B sandbox templates for coding agents (OpenCode, Goose)"
requires-python = ">=3.10"
dependencies = [
    "e2b>=2.3.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
dev-dependencies = []
