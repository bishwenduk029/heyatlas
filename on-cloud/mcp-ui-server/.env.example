VOICE_AGENT_URL=ws://localhost:8000
ENVIRONMENT=development

# Authentication
# REQUIRED: Set this to your production API key
# The MCP UI server will reject requests where the NIRMANUS_API_KEY header doesn't match this value
NIRMANUS_API_KEY=your_secure_api_key_here
