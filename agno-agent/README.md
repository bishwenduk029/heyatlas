# Agno Agent

WebSocket interface for Agno agent that replaces Goose integration in the voice agent.

## Features

- Agno agent with MCP tools integration
- FastAPI WebSocket interface
- Session management with libsql storage
- Compatible with existing voice agent structure

## Usage

```bash
# Install dependencies
uv install

# Start the server
uv run python main.py
```
