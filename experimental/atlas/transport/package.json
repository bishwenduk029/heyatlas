{
  "name": "@atlas/transport",
  "description": "PartyKit transport layer - WebSocket relay, HTTP proxy, and user credential management",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "partykit dev",
    "deploy": "partykit deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "partykit": "^0.0.105"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.8.2"
  },
  "engines": {
    "node": ">=20"
  }
}
